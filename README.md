# Coffee Shop Rush

Small static clicker game. Open `index.html` in a browser to run locally.

## Run locally
Open `index.html` in your browser or use a simple server:
- Python 3: `python3 -m http.server 8000` then visit http://localhost:8000

